{
  "author": "rpgja",
  "bugs": {
    "url": "https://github.com/rpgja/enrpg/issues"
  },
  "description": "Library on map data for RPGEN.",
  "devDependencies": {
    "@swc/jest": "^0.2.36",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "tsc-alias": "^1.8.10",
    "tsup": "^8.1.0",
    "typescript": "^5.5.3"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["LICENSE", "package.json", "dist"],
  "homepage": "https://github.com/rpgja/enrpg#readme",
  "keywords": ["rpgja", "rpgen"],
  "license": "MIT",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "name": "rpgen-map",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rpgja/enrpg.git"
  },
  "scripts": {
    "build": "bun run build:test && bun run build:tsup && bun run build:tsc && bun run build:tsc-alias",
    "build:test": "bun run test",
    "build:tsc": "tsc -p tsconfig.build.json",
    "build:tsc-alias": "tsc-alias -p tsconfig.build.json",
    "build:tsup": "tsup --tsconfig packages/rpgen-map/tsconfig.build.json",
    "prepare": "bun run build",
    "test": "jest"
  },
  "type": "module",
  "types": "./dist/index.d.ts",
  "version": "1.0.0"
}
